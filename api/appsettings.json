{
  "AzureAd": {
    "Instance": "https://login.microsoftonline.com/",
    "TenantId": "REPLACE-WITH-DIRECTORY-TENANT-ID",
    "ClientId": "REPLACE-WITH-API-APPLICATION-CLIENT-ID",

    "//Audience": "Defaults to api://<ClientId>. Set explicitly only if your tokens' 'aud' differs (e.g. a custom Application ID URI)."
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
